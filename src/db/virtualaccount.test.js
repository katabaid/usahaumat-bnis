const faker = require('faker')

const va = require('./virtualaccount')

describe('creating virtual account', () => {
  it('should create a new virtual account with a running id starting from 00 for each birthdates', () => {

  })

  it('should increment the running id when the same birthdates are entered', () => {

  })

  it('should generate an error when the running id exceeds 99', () => {

  })
})
